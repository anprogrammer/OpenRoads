Add instructions screen
Program Icon
Include server-side logging for fun stats

Add a settings screen:
-Uses old control screen faded out as background
-Sound Volume Slider
-Music Volume slider
-World Scale slider
-Selection between SnapshotProvider's
-Option for in-process audio (may cause lag)
-Background scale slider

Off a pixel on ship
Monitor selection or SDK improvements
Replace video with actual video

GO LIVE VERSION!

Fix the music emulator for more accurate sound

Fix "ANIM" missing ship parts